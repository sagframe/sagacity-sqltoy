# StringUtil 缺陷修复 — 精确代码变更清单

## 文件
`/sagacity-sqltoy/src/main/java/org/sagacity/sqltoy/utils/StringUtil.java`

测试文件:
`/sagacity-sqltoy/src/test/java/org/sagacity/sqltoy/utils/StringUtilsTest.java`

---

## 修复 1: `replaceAllStr` 边界判断 `>=` → `>` (缺陷1, 高)

### 变更
**行 ~1049** (`replaceAllStr(String source, String template, String target, int fromIndex, int endIndex)`)

注意: 当前代码已经是 `realFrom > realEnd`（L1058）。需要确认是否已修复。

当前 L1058:
```java
if (realFrom > realEnd) {
```
**状态: 已修复 ✓** — 代码中已经是 `>`，计划分析有误（可能基于旧版本）。

---

## 修复 2: offset 死循环防护 (缺陷2, 高)

### 变更 2a: `matchLastIndex` — L589-592
**旧代码:**
```java
		while (m.find(start)) {
			matchIndex = m.start();
			start = m.end() - offset;
		}
```
**新代码:**
```java
		while (m.find(start)) {
			matchIndex = m.start();
			start = Math.max(m.end() - offset, m.start() + 1);
		}
```

### 变更 2b: `matchCnt(source, pattern, offset)` — L630-633
**旧代码:**
```java
		while (matcher.find(start)) {
			count++;
			start = matcher.end() - offset;
		}
```
**新代码:**
```java
		while (matcher.find(start)) {
			count++;
			start = Math.max(matcher.end() - offset, matcher.start() + 1);
		}
```

### 变更 2c: `replaceRegex` — L1253-1260
**旧代码:**
```java
		while (matcher.find(start)) {
			count++;
			end = matcher.end();
			if (count == matchCnt) {
				return source.substring(0, matcher.start()) + replaceStr + source.substring(end);
			}
			start = end - offset;
		}
```
**新代码:**
```java
		while (matcher.find(start)) {
			count++;
			end = matcher.end();
			if (count == matchCnt) {
				return source.substring(0, matcher.start()) + replaceStr + source.substring(end);
			}
			start = Math.max(end - offset, matcher.start() + 1);
		}
```

---

## 修复 3: null 安全守卫 (缺陷3, 高)

### 变更 3a: `getSymMarkIndex` — L334 之后
在方法体第一行 `Pattern pattern = null;` 之前插入:
```java
		if (source == null) {
			return -1;
		}
```

### 变更 3b: `getSymMarkMatchIndex` — L441 之后
在 `boolean symMarkIsEqual = ...` 之前插入:
```java
		if (source == null) {
			return -1;
		}
```

### 变更 3c: `getSymMarkReverseIndex` — 方法入口
找到 `getSymMarkReverseIndex` 方法，在 `int beginIndex = source.length() - endIndex;` 之前插入:
```java
		if (source == null) {
			return -1;
		}
```

### 变更 3d: `clearSymMarkContent` — L500 之后
在 `StringBuilder lastSql = new StringBuilder(sql);` 之前插入:
```java
		if (sql == null) {
			return null;
		}
```

### 变更 3e: `matchIndex(String source, Pattern pattern)` — L555
**旧代码:**
```java
	public static int matchIndex(String source, Pattern pattern) {
		Matcher m = pattern.matcher(source);
```
**新代码:**
```java
	public static int matchIndex(String source, Pattern pattern) {
		if (source == null) {
			return -1;
		}
		Matcher m = pattern.matcher(source);
```

### 变更 3f: `matchIndex(String source, Pattern pattern, int start)` — L563
**旧代码:**
```java
	public static int[] matchIndex(String source, Pattern pattern, int start) {
		if (source.length() <= start) {
```
**新代码:**
```java
	public static int[] matchIndex(String source, Pattern pattern, int start) {
		if (source == null || source.length() <= start) {
```

### 变更 3g: `matchCnt(source, regex, beginIndex, endIndex)` — L645
**旧代码:**
```java
	public static int matchCnt(String source, String regex, int beginIndex, int endIndex) {
		return matchCnt(source.substring(beginIndex, endIndex), Pattern.compile(regex), 0);
```
**新代码:**
```java
	public static int matchCnt(String source, String regex, int beginIndex, int endIndex) {
		if (source == null) {
			return 0;
		}
		return matchCnt(source.substring(beginIndex, endIndex), Pattern.compile(regex), 0);
```

### 变更 3g2: `matchCnt(source, regex, beginIndex, endIndex, offset)` — L649
**旧代码:**
```java
	public static int matchCnt(String source, String regex, int beginIndex, int endIndex, int offset) {
		return matchCnt(source.substring(beginIndex, endIndex), Pattern.compile(regex), offset);
	}
```
**新代码:**
```java
	public static int matchCnt(String source, String regex, int beginIndex, int endIndex, int offset) {
		if (source == null) {
			return 0;
		}
		return matchCnt(source.substring(beginIndex, endIndex), Pattern.compile(regex), offset);
	}
```

### 变更 3h: `indexOrder` — L660
**旧代码:**
```java
	public static int indexOrder(String source, String regex, int order) {
		int begin = 0;
```
**新代码:**
```java
	public static int indexOrder(String source, String regex, int order) {
		if (source == null) {
			return -1;
		}
		int begin = 0;
```

### 变更 3i: `str2ASCII` — L680
**旧代码:**
```java
	public static int[] str2ASCII(String str) {
		char[] chars = str.toCharArray(); // 把字符中转换为字符数组
```
**新代码:**
```java
	public static int[] str2ASCII(String str) {
		if (str == null) {
			return new int[0];
		}
		char[] chars = str.toCharArray(); // 把字符中转换为字符数组
```

### 变更 3j: `like` — L1124
**旧代码:**
```java
	public static boolean like(String source, String[] keywords) {
		int index = 0;
		for (String keyword : keywords) {
```
**新代码:**
```java
	public static boolean like(String source, String[] keywords) {
		if (source == null || keywords == null || keywords.length == 0) {
			return false;
		}
		int index = 0;
		for (String keyword : keywords) {
```

### 变更 3k: `matches(String source, String regex)` — L524
**旧代码:**
```java
	public static boolean matches(String source, String regex) {
		return matches(source, Pattern.compile(regex));
	}
```
**新代码:**
```java
	public static boolean matches(String source, String regex) {
		if (regex == null) {
			return false;
		}
		return matches(source, Pattern.compile(regex));
	}
```

### 变更 3l: `humpFieldNames` — L987-988
**旧代码:**
```java
		for (int i = 0, n = labelNames.length; i < n; i++) {
			aliasIndex = labelNames[i].indexOf(":");
```
**新代码:**
```java
		for (int i = 0, n = labelNames.length; i < n; i++) {
			if (labelNames[i] == null) {
				result[i] = null;
				continue;
			}
			aliasIndex = labelNames[i].indexOf(":");
```

---

## 修复 4: `loopAppendWithSign` null 元素 NPE (缺陷4, 高)

### 变更 — L292-297
**旧代码:**
```java
	public static String loopAppendWithSign(String source, String sign, int loopSize) {
		if (loopSize <= 0) {
			return "";
		}
		return String.join(sign, Collections.nCopies(loopSize, source));
	}
```
**新代码:**
```java
	public static String loopAppendWithSign(String source, String sign, int loopSize) {
		if (loopSize <= 0) {
			return "";
		}
		String item = (source == null) ? "" : source;
		return String.join(sign, Collections.nCopies(loopSize, item));
	}
```

---

## 修复 5: `getSymMarkMatchIndex` 正则元字符转义 (缺陷5, 高)

### 变更 — L444-445
**旧代码:**
```java
		Pattern startP = Pattern.compile(beginMarkSign);
		Pattern endP = Pattern.compile(endMarkSign);
```
**新代码:**
```java
		Pattern startP = Pattern.compile(Pattern.quote(beginMarkSign));
		Pattern endP = Pattern.compile(Pattern.quote(endMarkSign));
```

---

## 修复 6: `splitRegex` 补充 `.` 和 `|` 字面量处理 (缺陷6, 中)

### 变更 — L1156-1171
**旧代码:**
```java
		if ("?".equals(regex)) {
			result = source.split("\\?");
		} else if (",".equals(regex)) {
			result = source.split("\\,");
		} else if (";".equals(regex)) {
			result = source.split("\\;");
		} else if (":".equals(regex)) {
			result = source.split("\\:");
		} else if (regex.length() > 0 && "".equals(regex.trim())) {
			result = source.split("\\s+");
		} else if ("||".equals(regex)) {
			result = source.split("\\|{2}");
		} else if ("&&".equals(regex)) {
			result = source.split("\\&{2}");
		} else {
			result = source.split(regex);
		}
```
**新代码:**
```java
		if ("?".equals(regex)) {
			result = source.split("\\?");
		} else if (",".equals(regex)) {
			result = source.split("\\,");
		} else if (";".equals(regex)) {
			result = source.split("\\;");
		} else if (":".equals(regex)) {
			result = source.split("\\:");
		} else if (".".equals(regex)) {
			result = source.split("\\.");
		} else if ("|".equals(regex)) {
			result = source.split("\\|");
		} else if (regex.length() > 0 && "".equals(regex.trim())) {
			result = source.split("\\s+");
		} else if ("||".equals(regex)) {
			result = source.split("\\|{2}");
		} else if ("&&".equals(regex)) {
			result = source.split("\\&{2}");
		} else {
			result = source.split(regex);
		}
```

---

## 修复 7: `secureMask` 负数参数校验 (缺陷7, 中)

### 变更 — L922-926
**旧代码:**
```java
	public static String secureMask(Object value, int preLength, int tailLength, String maskStr) {
		if (value == null) {
			return null;
		}
		String tmp = value.toString();
```
**新代码:**
```java
	public static String secureMask(Object value, int preLength, int tailLength, String maskStr) {
		if (value == null) {
			return null;
		}
		preLength = Math.max(0, preLength);
		tailLength = Math.max(0, tailLength);
		String tmp = value.toString();
```

---

## 修复 8: `replaceFirstStr` null 替换串产生 "null" (缺陷8, 中)

### 变更 — L1015-1025
**旧代码:**
```java
	public static String replaceFirstStr(String source, String target, String replacement, int fromIndex) {
		if (source == null || target == null || target.isEmpty()) {
			return source;
		}
		// 从fromIndex位置开始查找
		int idx = source.indexOf(target, fromIndex);
		if (idx == -1) {
			return source;
		}
		return source.substring(0, idx) + replacement + source.substring(idx + target.length());
	}
```
**新代码:**
```java
	public static String replaceFirstStr(String source, String target, String replacement, int fromIndex) {
		if (source == null || target == null || target.isEmpty()) {
			return source;
		}
		// 从fromIndex位置开始查找
		int idx = source.indexOf(target, fromIndex);
		if (idx == -1) {
			return source;
		}
		String rep = (replacement == null) ? "" : replacement;
		return source.substring(0, idx) + rep + source.substring(idx + target.length());
	}
```

---

## 修复 9: 静态 Pattern 字段添加 `final` (缺陷9, 低)

### 变更 — L27, 32, 34, 39, 41
将 5 个字段声明从 `private static Pattern` 改为 `private static final Pattern`:
```java
	private static final Pattern chinaPattern = Pattern.compile("[\u4e00-\u9fa5]");
	private static final Pattern quotaPattern = Pattern.compile("(^')|([^\\\\]')");
	private static final Pattern quotaChkPattern = Pattern.compile("[^\\\\]'");
	private static final Pattern twoQuotaPattern = Pattern.compile("(^\")|([^\\\\]\")");
	private static final Pattern twoQuotaChkPattern = Pattern.compile("[^\\\\]\"");
```

---

## 修复 10: `splitExcludeSymMark` 原始类型 ArrayList → 泛型 (缺陷10, 低)

### 变更 — L715
**旧代码:**
```java
		ArrayList splitResults = new ArrayList();
```
**新代码:**
```java
		ArrayList<String> splitResults = new ArrayList<String>();
```

同时可以简化 L746-750 的转型（可选）:
```java
		return splitResults.toArray(new String[0]);
```
替换:
```java
		int resultSize = splitResults.size();
		String[] resultStr = new String[resultSize];
		for (int j = 0; j < resultSize; j++) {
			resultStr[j] = (String) splitResults.get(j);
		}
		return resultStr;
```

---

## 修复 11: 参数名拼写 `soure` → `source` (缺陷11, 低)

### 变更 — L99-103 和 L106-110
将两个方法中的 `soure` 全部替换为 `source`:

```java
	public static boolean trimedEquals(String source, String target) {
		if (source == null || target == null) {
			return source == target;
		}
		return source.trim().equals(target.trim());
	}

	public static boolean trimedIgnoreCaseEquals(String source, String target) {
		if (source == null || target == null) {
			return source == target;
		}
		return source.trim().equalsIgnoreCase(target.trim());
	}
```

⚠️ **注意**: 这是公开 API，调用方传参数名引用会受影响。需全局搜索确认无按名引用的调用方。

---

## 修复 12: `toDBC` 优化为单次遍历 (缺陷12, 低)

### 变更 — L1082-1090
**旧代码:**
```java
	public static String toDBC(String SBCStr) {
		if (isBlank(SBCStr)) {
			return SBCStr;
		}
		// 常用符号进行全角转半角
		return SBCStr.replaceAll("\\；", ";").replaceAll("\\？", "?").replaceAll("\\．", ".").replaceAll("\\：", ":")
				.replaceAll("\\＇", "'").replaceAll("\\＂", "\"").replaceAll("\\，", ",").replaceAll("\\【", "[")
				.replaceAll("\\】", "]").replaceAll("\\）", ")").replaceAll("\\（", "(").replaceAll("\\＝", "=");
	}
```
**新代码:**
```java
	public static String toDBC(String SBCStr) {
		if (isBlank(SBCStr)) {
			return SBCStr;
		}
		char[] chars = SBCStr.toCharArray();
		StringBuilder sb = new StringBuilder(chars.length);
		for (char c : chars) {
			switch (c) {
				case '；': sb.append(';'); break;
				case '？': sb.append('?'); break;
				case '．': sb.append('.'); break;
				case '：': sb.append(':'); break;
				case '＇': sb.append('\''); break;
				case '＂': sb.append('"'); break;
				case '，': sb.append(','); break;
				case '【': sb.append('['); break;
				case '】': sb.append(']'); break;
				case '）': sb.append(')'); break;
				case '（': sb.append('('); break;
				case '＝': sb.append('='); break;
				default: sb.append(c); break;
			}
		}
		return sb.toString();
	}
```

---

## 缺陷 13: `humpToSplitStr` 连续大写边界 — 不修改

已知局限，单独 story 处理。当前行为不影响核心 ORM 功能。

---

## 测试新增

在 `StringUtilsTest.java` 中添加以下测试:

```java
	@Test
	@org.junit.jupiter.api.Timeout(2)
	public void testMatchCntOffsetInfiniteLoop() {
		// 缺陷2: offset >= 匹配长度时不应死循环
		assertEquals(1, StringUtil.matchCnt("aaa", Pattern.compile("aa"), 2));
	}

	@Test
	public void testNullSafety() {
		// 缺陷3: null 安全
		assertEquals(-1, StringUtil.getSymMarkIndex("(", ")", null, 0));
		assertEquals(-1, StringUtil.getSymMarkMatchIndex("(", ")", null, 0));
		assertEquals(-1, StringUtil.getSymMarkReverseIndex("(", ")", null, 1));
		assertNull(StringUtil.clearSymMarkContent(null, "(", ")"));
		assertEquals(-1, StringUtil.matchIndex(null, Pattern.compile("a")));
		assertArrayEquals(new int[]{-1,-1}, StringUtil.matchIndex(null, Pattern.compile("a"), 0));
		assertEquals(0, StringUtil.matchCnt(null, "a", 0, 1));
		assertEquals(-1, StringUtil.indexOrder(null, "a", 0));
		assertArrayEquals(new int[0], StringUtil.str2ASCII(null));
		assertFalse(StringUtil.like(null, new String[]{"a"}));
		assertFalse(StringUtil.matches("abc", (String) null));
	}

	@Test
	public void testLoopAppendWithSignNullSource() {
		// 缺陷4: source=null 不应抛NPE
		String result = StringUtil.loopAppendWithSign(null, ",", 3);
		assertEquals(",,", result);
	}

	@Test
	public void testSymMarkMatchIndexWithRegexChars() {
		// 缺陷5: 正则元字符作为标记符号不应抛异常
		int idx = StringUtil.getSymMarkMatchIndex("(", ")", "a(b+c)", 0);
		assertTrue(idx > 0);
	}

	@Test
	public void testSplitRegexDotAndPipe() {
		// 缺陷6: "." 和 "|" 作为字面量分隔符
		assertArrayEquals(new String[]{"1","2","3"}, StringUtil.splitRegex("1.2.3", ".", false));
		assertArrayEquals(new String[]{"a","b"}, StringUtil.splitRegex("a|b", "|", false));
	}

	@Test
	public void testSecureMaskNegativeParams() {
		// 缺陷7: 负数参数不应抛异常
		String result = StringUtil.secureMask("hello", -1, 2, "***");
		assertNotNull(result);
	}

	@Test
	public void testReplaceFirstStrNullReplacement() {
		// 缺陷8: null 替换串应为删除而非插入"null"
		assertEquals("ac", StringUtil.replaceFirstStr("abc", "b", null));
	}

	@Test
	public void testHumpFieldNamesWithNullElement() {
		// 缺陷3l: 数组含 null 元素不应 NPE
		String[] result = StringUtil.humpFieldNames(new String[]{"a_b", null, "c_d"});
		assertEquals("aB", result[0]);
		assertNull(result[1]);
		assertEquals("cD", result[2]);
	}
```

---

## 实施顺序

1. 先做 `StringUtil.java` 的所有代码修改（修复 2-12）
2. 添加测试到 `StringUtilsTest.java`
3. `eclipseBuildProject("sagacity-sqltoy")` 验证编译
4. `eclipseRunTests("sagacity-sqltoy", "org.sagacity.sqltoy.utils.StringUtilsTest")` 运行测试
5. 确认全部通过后完成

## Open Questions
1. **`splitRegex` default 分支语义**: 保持 default 为正则，仅补充 `.` 和 `|` 字面量特例。✅ 已决定。
2. **`replaceFirstStr` null 替换语义**: 视为删除（空串）。✅ 已决定。
3. **缺陷13**: 暂不修改。✅ 已决定。
